@A("\0\0662666z3")
permit(
  principal,
  action == Action::"6666+6666666e666666666;6",
  resource
) when {
  ((true && ([[action, action, action], [], []].contains([resource, resource, resource]))) && false) && false
};
