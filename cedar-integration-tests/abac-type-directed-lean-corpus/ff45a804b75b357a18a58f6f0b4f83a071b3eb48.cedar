permit(
  principal,
  action == A::Action::"action",
  resource in A::a::""
) when {
  ((true && (resource in resource)) && (resource in resource)) && ((if (principal in principal) then (if true then (decimal("-50174297822398.8481")) else (decimal("0.0"))) else (decimal("0.0"))) == (decimal("0.0")))
};
