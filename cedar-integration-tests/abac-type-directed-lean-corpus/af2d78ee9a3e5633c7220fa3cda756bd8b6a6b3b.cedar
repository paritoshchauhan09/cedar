forbid(
  principal is Ie in ye::"",
  action in [Action::"action",Action::"action",Action::"action"],
  resource
) when {
  ((true && (action in action)) && (principal is xd)) && (principal in principal)
};
