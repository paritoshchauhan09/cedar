permit(
  principal == a::"",
  action,
  resource
) when {
  ((true && ([[principal, principal], [principal, principal], [principal, principal]].containsAll([J::JDopDDrnr50000::A::"", principal]))) && false) && false
};
