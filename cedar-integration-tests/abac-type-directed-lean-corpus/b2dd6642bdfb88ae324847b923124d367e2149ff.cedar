@Ahh00("")
forbid(
  principal,
  action in [Mi::Bsjj::Action::"action"],
  resource
) when {
  ((true && ((principal in principal) == (principal in principal))) && (principal in principal)) && (principal in principal)
};
