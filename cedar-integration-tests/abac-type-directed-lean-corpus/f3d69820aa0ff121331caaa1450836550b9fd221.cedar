permit(
  principal == a::"",
  action,
  resource
) when {
  ((true && ([[principal, principal], [principal, principal], [[]]].containsAll(if false then [] else []))) && true) && (a::"MCC>665" in a::"")
};
