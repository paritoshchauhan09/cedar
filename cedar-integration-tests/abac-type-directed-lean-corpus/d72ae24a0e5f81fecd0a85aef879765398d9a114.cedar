forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (if ([].containsAll([[]])) then (a::"A333333333A6" in Action::"action") else (!([].containsAll([]))))) && ([[]].containsAll([]))) && true
};
