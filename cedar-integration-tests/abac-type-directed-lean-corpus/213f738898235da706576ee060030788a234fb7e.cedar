permit(
  principal,
  action,
  resource == a::""
) when {
  ((true && ((if ((decimal("0.1")) == (decimal("0.1"))) then (decimal("##")) else (decimal(""))) == (decimal("#####")))) && ((action has "") == true)) && false
};
