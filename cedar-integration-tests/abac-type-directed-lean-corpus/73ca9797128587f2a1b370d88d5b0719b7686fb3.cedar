permit(
  principal == a::"6663DDDDDDDDDD",
  action,
  resource == a::"6663DDDDDDDDDD"
) when {
  ((true && ((if (principal in principal) then (decimal("\0")) else (if true then (decimal("0.1")) else (decimal("0.1")))) == (if ((decimal("0.1")) == (decimal("-72057594037792.7936"))) then (decimal("0.0")) else (decimal("0.0"))))) && false) && false
};
