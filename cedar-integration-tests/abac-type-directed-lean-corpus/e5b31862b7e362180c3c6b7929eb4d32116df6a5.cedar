forbid(
  principal is a in a::"\0\0\0\0\0",
  action == Action::"action",
  resource
) when {
  ((true && ((if ((decimal("0.0")) == (decimal("0.0"))) then (if true then (decimal("0.0")) else (decimal("0.0"))) else (decimal("33"))) == (decimal("33")))) && false) && false
};
