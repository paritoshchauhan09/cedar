permit(
  principal is a in a::"66666,66",
  action == Action::"action",
  resource is a
) when {
  (true && (resource has "Bsss")) && ((if ((decimal("0.1")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))
};
