permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if (false == false) then (decimal("consl_c")) else (decimal("0.111"))) == (decimal("0.0")))) && false) && false
};
