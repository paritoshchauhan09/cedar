forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if (false == false) then (decimal("")) else (if true then (decimal("0.1")) else (decimal("0.1")))) == (decimal("0.1")))) && true) && true
};
