forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if ((decimal("0.1")) == (decimal("0.1"))) then (decimal("0.1")) else (decimal("0.1"))) == (decimal("0.1")))) && false) && false
};
