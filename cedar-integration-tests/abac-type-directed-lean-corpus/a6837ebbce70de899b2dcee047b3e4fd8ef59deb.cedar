forbid(
  principal is a in a::"\0",
  action in [Action::"action",Action::"action",Action::"action"],
  resource
) when {
  ((true && ([[]].containsAll([[2644352408], if true then [] else [], resource["Bsss"]]))) && (a::"\0" in principal)) && (principal has "M\0\0\0\0\0\0\0\0\0")
};
