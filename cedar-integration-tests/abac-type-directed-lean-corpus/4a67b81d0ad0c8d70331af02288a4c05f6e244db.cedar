forbid(
  principal is A::a in A::a::"",
  action in [A::Action::"action"],
  resource == A::a::""
) when {
  ((true && ((if ((decimal("106956477.1894")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
