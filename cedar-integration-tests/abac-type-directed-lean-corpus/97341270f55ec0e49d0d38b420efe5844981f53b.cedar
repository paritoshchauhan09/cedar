permit(
  principal is A::A::a in A::A::a::">666\0",
  action in [A::A::Action::"action",A::A::Action::"action"],
  resource is A::A::a in A::A::a::">666\0"
) when {
  ((true && (if (lsssssssi_ss::Bka0::a::"" in principal) then false else false)) && false) && false
};
