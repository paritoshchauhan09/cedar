forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if ((decimal("0.1")) == (decimal("0.255"))) then (ip("0.0.0.0")) else (ip("0.0.0.0"))) == (ip("0.0.0.0")))) && false) && false
};
