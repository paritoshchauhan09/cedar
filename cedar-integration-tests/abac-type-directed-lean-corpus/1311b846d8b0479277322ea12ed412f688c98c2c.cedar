forbid(
  principal is A::a in A::a::"",
  action in [A::Action::"action"],
  resource
) when {
  ((true && ((if ((decimal("3961147222515600.5887")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))).lessThanOrEqual(decimal("0.0")))) && false) && false
};
