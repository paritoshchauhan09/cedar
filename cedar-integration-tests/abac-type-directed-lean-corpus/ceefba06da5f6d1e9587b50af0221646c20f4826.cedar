permit(
  principal,
  action in [Action::"action",Action::"action"],
  resource == a::""
) when {
  ((true && ((if ((-958113251328) < 255) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
