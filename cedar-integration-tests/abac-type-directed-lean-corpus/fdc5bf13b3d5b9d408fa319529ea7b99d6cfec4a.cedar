@C("")
@r("")
permit(
  principal,
  action,
  resource
) when {
  ((true && ((if ((decimal("-1395.9169")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
