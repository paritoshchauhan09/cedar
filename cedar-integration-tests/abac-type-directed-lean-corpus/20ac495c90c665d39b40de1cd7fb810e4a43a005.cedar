forbid(
  principal,
  action in [Action::"action"],
  resource is a in a::""
) when {
  ((true && ((decimal(principal["KAAAA"])) == (decimal(action["KAAAA"])))) && ((decimal(principal["KAAAA"])).greaterThan(decimal(principal["KAAAA"])))) && (principal in principal)
};
