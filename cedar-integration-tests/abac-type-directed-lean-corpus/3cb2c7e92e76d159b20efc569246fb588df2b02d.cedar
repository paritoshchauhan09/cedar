permit(
  principal,
  action,
  resource == a::"\0\0\0~6"
) when {
  ((true && ("" == (if false then (if false then (decimal("0.320")) else (decimal("0.255"))) else (decimal("0.0"))))) && false) && false
};
