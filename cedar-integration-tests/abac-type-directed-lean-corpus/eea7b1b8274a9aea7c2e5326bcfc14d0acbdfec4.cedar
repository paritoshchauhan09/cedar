permit(
  principal is a in a::"663",
  action in [Action::"action"],
  resource == a::"663"
) when {
  (true && ((decimal(principal["Bppp"])).lessThan(decimal(principal["Bppp"])))) && ((decimal("0.0")) == (decimal("0.0")))
};
