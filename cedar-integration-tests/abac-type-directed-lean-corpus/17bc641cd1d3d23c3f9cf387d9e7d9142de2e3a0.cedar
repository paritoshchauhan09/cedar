forbid(
  principal is a in a::"\0",
  action in [Action::"action"],
  resource == a::"\0"
) when {
  ((true && ((if ((decimal("382158576309554.3807")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
