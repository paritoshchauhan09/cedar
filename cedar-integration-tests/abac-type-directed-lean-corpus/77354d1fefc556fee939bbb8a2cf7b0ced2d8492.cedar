permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if ([] == []) then (decimal("6\0\0")) else (decimal(""))) == (if (!true) then (if true then (decimal("01")) else (decimal("0.0"))) else (decimal("0.0"))))) && false) && false
};
