forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (true == (a::"\0\0\0\u{1}" is a))) && ((if (59609837876904 == 0) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false
};
