permit(
  principal,
  action == Action::"action",
  resource is a
) when {
  ((true && ((if true then (decimal("-99343447133227.5146")) else (decimal("0.0"))).greaterThan(decimal("0.0")))) && false) && false
};
