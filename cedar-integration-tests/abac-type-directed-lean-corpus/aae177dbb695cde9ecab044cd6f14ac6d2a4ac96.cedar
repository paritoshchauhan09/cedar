permit(
  principal == a::"",
  action,
  resource
) when {
  ((true && ([[principal, principal], [principal, principal], [3906378129349162550, 155394127420]].containsAll(if (principal is a) then [] else []))) && false) && false
};
