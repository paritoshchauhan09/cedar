forbid(
  principal == a::"\0",
  action in [Action::"action"],
  resource
) when {
  ((true && ((if (action in principal) then (if false then (decimal("353082210868055.2448")) else (decimal("0.0"))) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
