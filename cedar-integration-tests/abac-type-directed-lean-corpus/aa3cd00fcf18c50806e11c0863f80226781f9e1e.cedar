forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (({"tiotion############################################################################r\"###`################_conn_&>ns": principal, "": false}["tiotion############################################################################r\"###`################_conn_&>ns"]) in principal)) && false) && false
};
