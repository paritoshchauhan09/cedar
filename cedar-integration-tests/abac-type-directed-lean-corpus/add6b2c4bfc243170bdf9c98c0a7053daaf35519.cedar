permit(
  principal is r::B333sFM::r::a in r::B333sFM::r::a::"pr666",
  action in [r::B333sFM::r::Action::"action"],
  resource is r::B333sFM::r::a
) when {
  (true && (!((resource["zpd8"]) <= (resource["zpd8"])))) && (resource in resource)
};
