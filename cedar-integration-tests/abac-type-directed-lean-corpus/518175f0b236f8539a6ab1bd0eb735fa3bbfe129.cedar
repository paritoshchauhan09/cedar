permit(
  principal,
  action in [Action::"action",Action::"action"],
  resource
) when {
  ((true && ((if ((decimal("109951162.7775")) == (decimal("0.0"))) then (decimal("0.0")) else (decimal("0.0"))) == (decimal("0.0")))) && false) && false
};
