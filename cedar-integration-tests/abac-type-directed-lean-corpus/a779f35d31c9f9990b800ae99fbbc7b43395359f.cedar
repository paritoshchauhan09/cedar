permit(
  principal,
  action,
  resource is a
) when {
  (true && (action in [a::",????????????????????????????????????????????????????", action, a::"?????????????????????????????????"])) && ((if ((decimal("0.1")) == (decimal("0.1"))) then (if true then (decimal("0.1")) else (decimal("0.1"))) else (decimal("\0"))) == (decimal("50440315826549.5551")))
};
