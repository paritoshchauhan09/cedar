permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((if ([] == []) then (decimal("consl_")) else (decimal("12584447839.115"))) == (decimal("0.0")))) && false) && false
};
