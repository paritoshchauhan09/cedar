forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && ((resource in resource) == ((if true then (decimal("0.1")) else (decimal("0.1"))) == (decimal("QQQQQQQQQQQQQQQQQQQ"))))) && true) && true
};
