permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  (true && ((decimal("")).greaterThan(if true then (if false then (decimal("64874044608303.2630")) else (decimal("0.0"))) else (decimal("0.0"))))) && false
};
