forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource
) when {
  true && ((if ((decimal("0.1")) == (decimal("0.1"))) then (if false then (decimal("0.256")) else (decimal("0.1"))) else (if true then (decimal("0.1")) else (decimal("0.1")))) == (if ((decimal("0.1")) == (decimal("-5.8625"))) then (decimal("332")) else (decimal(""))))
};
