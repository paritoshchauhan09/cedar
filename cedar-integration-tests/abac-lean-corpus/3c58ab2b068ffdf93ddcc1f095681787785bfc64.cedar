forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (a::"" in [a::"", Action::"action", context, AShh::"j", A::"", principal, principal])) && principal) && principal
};
