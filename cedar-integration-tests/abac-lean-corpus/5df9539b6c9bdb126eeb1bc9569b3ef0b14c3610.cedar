permit(
  principal == a::"\0\0\0",
  action in [Action::"action"],
  resource in a::"\0\0\0"
) when {
  ((true && ([principal, principal, principal, principal, Ahhch::"", a::"\0\0\0", [principal, principal, principal, principal, principal, Achh0::"\0\0", false]] == [A::"", principal])) && principal) && principal
};
