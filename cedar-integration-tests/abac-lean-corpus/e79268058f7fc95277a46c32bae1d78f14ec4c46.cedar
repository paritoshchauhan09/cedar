forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource == a::""
) when {
  ((true && (("" in ("" in "")) == ((principal in principal) in principal))) && principal) && principal
};
