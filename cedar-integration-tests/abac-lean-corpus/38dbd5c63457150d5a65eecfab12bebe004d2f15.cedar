@r("\u{3}L\0")
permit(
  principal == a::"\0\0\0\0\0",
  action in [Action::"action"],
  resource in a::""
) when {
  (true && Ahchh::"") && (if (if (Action::"action" in Action::"action") then (G::"" in Action::"action") else (if action then 7854380861518800201 else principal)) then false else (if context then (false == principal) else false))
};
