forbid(
  principal == r::k::r3::r::r::r::r::Ahhcx::"",
  action in [r::k::r3::r::r::r::r::Action::"action"],
  resource
) when {
  ((true && action) && (if (!(!false)) then "\0j" else [!false, action, principal.containsAll(principal), principal, principal, principal, principal])) && principal
};
