permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  (true && (([] == Q0008::_gglDZhhhmh::"pj").containsAny([Action::"action", action]))) && (((action == principal) == principal) == principal)
};
