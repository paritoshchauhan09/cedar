forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource
) when {
  ((true && ((context in (Action::"action" in Action::"action")) in ((action || action) in (action || action)))) && (((action || action) || (action || action)) || ((action || action) || (action || action)))) && ((Action::"action" in (action || action)) || ((action || action) || principal))
};
