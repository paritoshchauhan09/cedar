forbid(
  principal is a in a::"",
  action in [Action::"",Action::"",Action::""],
  resource is a in a::""
) when {
  ((true && (((if principal then principal else principal) has "j\u{c}`jjjjjjj") has "")) && principal) && principal
};
