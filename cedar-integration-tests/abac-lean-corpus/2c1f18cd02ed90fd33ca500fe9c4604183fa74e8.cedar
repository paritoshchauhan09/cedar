permit(
  principal is jwwwSShhhh::a in jwwwSShhhh::a::"",
  action in [jwwwSShhhh::Action::"action",jwwwSShhhh::Action::"action",jwwwSShhhh::Action::"action"],
  resource
) when {
  ((true && principal) && principal) && principal
};
