permit(
  principal is a in a::"",
  action,
  resource
) when {
  true && ([Action::"action", Action::"action" in Action::"action"].containsAny([a::"", principal, action, context, a::"z", a::"z", context]))
};
