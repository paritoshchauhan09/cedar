@K0("")
permit(
  principal,
  action in [Action::"action",Action::"action"],
  resource == a::""
) when {
  ((true && [[principal, principal, principal, principal, principal, principal, principal] == principal, principal, principal, principal, principal, principal, principal]) && principal) && principal
};
