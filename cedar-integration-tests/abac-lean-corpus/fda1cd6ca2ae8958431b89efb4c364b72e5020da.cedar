forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (a::"" in [action, [r::A::r::"", Ah000000000::""], principal, principal, principal, principal, principal])) && principal) && principal
};
