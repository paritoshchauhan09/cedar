permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  (true && [i::"", a::"jjjjj", (context == context) == (context == context)]) && (((context == context) == (context == context)) == ((context == action) == (!(resource <= action))))
};
