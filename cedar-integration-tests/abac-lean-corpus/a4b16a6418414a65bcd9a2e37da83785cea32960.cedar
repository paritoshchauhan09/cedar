permit(
  principal is a in a::"\0\0\0",
  action == Action::"action",
  resource
) when {
  ((true && (((true == resource) == (resource || resource)) <= ((resource || resource) || (resource || resource)))) && (((resource || resource) || (resource || resource)) || ((resource || resource) || (resource || resource)))) && (((action || resource) || principal) || principal)
};
