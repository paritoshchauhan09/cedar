permit(
  principal == a::"\0",
  action in [Action::"action"],
  resource in a::"\0"
) when {
  ((true && ([a::"\0", principal, context, a::"\0", principal, Ahhch::"", a::"\0"] == [action, wLHLN000Ovhjhhh::"", resource, [principal, principal, principal, principal, principal, principal, principal], principal, principal, principal])) && principal) && principal
};
