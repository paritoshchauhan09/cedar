@jwwwwwwWWh("njr")
forbid(
  principal in a::"",
  action in [Action::"action",Action::"action",Action::"action"],
  resource in a::""
) when {
  ((true && ((("                        " == principal) == A::"") == principal)) && principal) && principal
};
