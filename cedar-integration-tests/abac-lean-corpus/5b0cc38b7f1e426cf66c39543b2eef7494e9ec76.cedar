permit(
  principal is a in a::"\0",
  action in [Action::"action"],
  resource
) when {
  ((true && (((true == true) == (action == Action::"action")) == true)) && (((principal == principal) == principal) == principal)) && principal
};
