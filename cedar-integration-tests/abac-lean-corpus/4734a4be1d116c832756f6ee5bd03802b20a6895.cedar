permit(
  principal,
  action in [Action::"action"],
  resource is a
) when {
  ((true && (((principal == principal) == (principal == principal)) == ((principal == principal) == (principal == principal)))) && (((principal == principal) == (principal == principal)) == (principal == principal))) && principal
};
