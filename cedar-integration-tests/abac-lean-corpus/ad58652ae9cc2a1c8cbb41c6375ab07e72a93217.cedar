@r("\u{3}L\0")
permit(
  principal == a::"\0\0\0\0\0",
  action in [Action::"action",Action::"action",Action::"action"],
  resource
) when {
  ((true && (!Ahchh::"")) && (if (if (Action::"action" in true) then principal else action) then (!principal) else (if false then false else (if "\u{1d}" then principal else principal)))) && principal
};
