permit(
  principal == a::"j",
  action in [Action::"nks-`"],
  resource is a in a::"j"
) when {
  (true && [context.greaterThan(Action::"nks-`" == false), principal, principal, principal, principal, principal]) && principal
};
