permit(
  principal == a::"",
  action,
  resource is a
) when {
  ((true && (((resource == true) == (context has "wwwwww")) has "wwwwwwwww")) && (((principal == principal) == principal) == principal)) && principal
};
