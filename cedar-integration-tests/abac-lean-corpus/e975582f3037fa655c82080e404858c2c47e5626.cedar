forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (a::"" in [a::"", Action::"action", context, Ahh00000000::AhhH::"", principal, principal, principal])) && principal) && principal
};
