permit(
  principal == a::"jj",
  action in [Action::"nks-`"],
  resource is a in a::"jj"
) when {
  (true && [action.greaterThan(action), !(!context), principal, (principal == principal) == (true == a::"jj"), principal, principal]) && (!Action::"nks-`")
};
