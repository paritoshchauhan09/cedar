permit(
  principal == _3hh::"",
  action in [Action::""],
  resource
) when {
  (true && (!((!((!(true < true)) < (!(true < true)))) < (!((!(true < true)) < (!(true < true))))))) && (!((!((if principal then principal else principal) < principal)) < principal))
};
