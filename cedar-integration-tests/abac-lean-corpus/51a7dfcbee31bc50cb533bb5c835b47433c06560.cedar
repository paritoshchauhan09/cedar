permit(
  principal == a::"\0\0\0",
  action in [Action::"action"],
  resource in a::"\0\0\0"
) when {
  ((true && ([true, resource, [principal, principal, principal, context, principal, action, false], false, [A::KcLHL::"", false], [A0hg1::"\0\0", false], principal] == principal)) && context) && action
};
