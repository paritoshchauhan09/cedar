permit(
  principal == a::"\0\0\0",
  action in [Action::"action"],
  resource in a::"\0\0\0"
) when {
  ((true && ([principal, principal, principal, principal, Ahhcg::"", action, A0000::"\0\0\0\0\0"] == principal)) && principal) && principal
};
