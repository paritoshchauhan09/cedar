permit(
  principal is a in a::"",
  action,
  resource
) when {
  true && ([Action::"action", Action::"action" in Action::"action"].containsAny([a::"", action, a::"", Action::"action", a::"z", context, action in Action::"action"]))
};
