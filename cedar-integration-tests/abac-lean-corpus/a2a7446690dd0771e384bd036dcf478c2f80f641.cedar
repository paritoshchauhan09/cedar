@Cv0("\0")
permit(
  principal,
  action == Action::"action",
  resource == a::""
) when {
  (true && [(!a::"").greaterThan(Action::"action" && false), C0000::"\0\0\0\0", principal, principal, principal, a::"llll\0`"]) && Action::"action"
};
