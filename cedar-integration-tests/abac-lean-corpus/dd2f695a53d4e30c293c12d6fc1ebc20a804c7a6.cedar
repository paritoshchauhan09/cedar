permit(
  principal == a::"\0",
  action in [Action::"action"],
  resource in a::"\0"
) when {
  ((true && ([106, principal, principal, principal, principal, principal, principal] == principal)) && principal) && principal
};
