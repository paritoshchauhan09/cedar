forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource is a in a::"kn"
) when {
  ((true && (if false then "Q" else {})) && (if principal then ((a::"" == "QQ") < {}) else (!(Ak000::"" < principal)))) && principal
};
