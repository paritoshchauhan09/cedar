forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && [[AhtcX::"jjjfb", Action::"action".containsAll(Action::"action")], [[a::"", principal], principal]]) && principal) && principal
};
