permit(
  principal is a in a::"j",
  action,
  resource == a::"j"
) when {
  true && ([Action::"action", Action::"action" in Action::"action"].containsAny([principal, false, principal, principal, principal, principal, principal]))
};
