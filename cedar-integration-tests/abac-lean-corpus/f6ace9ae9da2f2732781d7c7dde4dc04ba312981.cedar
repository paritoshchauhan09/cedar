permit(
  principal,
  action,
  resource
) when {
  (true && ((["\u{1d}\u{1d}", Action::"action", AhhGh::"", a::"", Achgh::""] == [Action::"action", A::"", true, principal, principal, principal]) in principal)) && principal
};
