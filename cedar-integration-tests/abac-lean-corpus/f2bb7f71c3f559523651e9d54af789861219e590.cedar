permit(
  principal,
  action in [Action::"action"],
  resource
) when {
  (true && [i::"j\u{13}g", (context == context) == (context == context), (context == context) == (context == context)]) && (((principal == principal) == principal) == principal)
};
