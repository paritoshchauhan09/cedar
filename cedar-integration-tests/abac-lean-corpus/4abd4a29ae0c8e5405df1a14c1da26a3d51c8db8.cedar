@AF0xj("j.j]")
@K("")
@r("")
@r3333("")
forbid(
  principal == a::"",
  action in [Action::"action"],
  resource
) when {
  ((true && principal) && (((context == principal) == (principal == principal)) == ((principal == principal) == (principal == context)))) && (a::"" == (a::"" == (principal == principal)))
};
