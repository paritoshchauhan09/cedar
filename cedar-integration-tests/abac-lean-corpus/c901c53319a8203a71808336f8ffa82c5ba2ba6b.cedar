@A02h000hhzbiiiih("`")
permit(
  principal is a,
  action in [Action::"`",Action::"`"],
  resource in a::"."
) when {
  ((true && (!((resource in (principal["_hhq"])) < action))) && (((W00000000000::"" == principal) == principal) == principal)) && principal
};
