forbid(
  principal,
  action in [Action::"action",Action::"action",Action::"action"],
  resource is a in a::"idjj"
) when {
  (true && (((principal in principal) in principal) in principal)) && principal
};
